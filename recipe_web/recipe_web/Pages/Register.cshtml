@page
@model recipe_web.Pages.RegisterModel
@{
    ViewData["Title"] = "Register";
    Layout = "~/Pages/Shared/_LayoutLogin.cshtml";
}

<div class="container">
    <div class="panel">
        <h1>Register</h1>
        <form method="post">
            <div class="form_input">
                <label asp-for="RegisterWebDTO.Username"></label>
                <input asp-for="RegisterWebDTO.Username" class="form-control" />
                <span asp-validation-for="RegisterWebDTO.Username" class="text-danger"></span>
            </div>
            <div class="form_input">
                <label asp-for="RegisterWebDTO.Email"></label>
                <input asp-for="RegisterWebDTO.Email" class="form-control" />
                <span asp-validation-for="RegisterWebDTO.Email" class="text-danger"></span>
            </div>
            <div class="form_input">
                <label asp-for="RegisterWebDTO.Password"></label>
                <input asp-for="RegisterWebDTO.Password" type="password" class="form-control" />
                <span asp-validation-for="RegisterWebDTO.Password" class="text-danger"></span>
            </div>
            <button type="submit" class="button">Register</button>
        </form>
        <div class="login_footer">
            <p>You already have an account? <br> <a asp-page="/Login">Login</a></p>
        </div>
    </div>
</div>

@section Scripts {
    <partial name="_ValidationScriptsPartial" />
}
