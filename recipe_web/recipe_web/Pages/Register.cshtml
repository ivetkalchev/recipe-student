@page
@model recipe_web.Pages.RegisterModel
@{
    ViewData["Title"] = "Register";
    Layout = "~/Pages/Shared/_LayoutLogin.cshtml";
}

<div class="container">
    <div class="panel">
        <h1>Register</h1>
        <form method="post">
            <div class="form_input">
                <label asp-for="RegisterDTO.Username">Username</label>
                <input asp-for="RegisterDTO.Username" required />
                <span asp-validation-for="RegisterDTO.Username" class="text-danger"></span>
            </div>
            <div class="form_input">
                <label asp-for="RegisterDTO.Password">Password</label>
                <input asp-for="RegisterDTO.Password" type="password" required />
                <span asp-validation-for="RegisterDTO.Password" class="text-danger"></span>
            </div>
            <div class="form_input">
                <label asp-for="RegisterDTO.Email">Email</label>
                <input asp-for="RegisterDTO.Email" required />
                <span asp-validation-for="RegisterDTO.Email" class="text-danger"></span>
            </div>
            <button type="submit" class="button">Register</button>
        </form>
        <div class="login_footer">
            <p>You already have an account? <br> <a asp-page="/Login">Login</a></p>
        </div>
    </div>
</div>
